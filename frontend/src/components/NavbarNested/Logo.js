// frontend/src/components/NavbarNested/Logo.js
import React from 'react';
import { Text } from '@mantine/core';

// Substitua por seu logo real (imagem ou SVG) se tiver
export function Logo(props) {
  return (
    <Text fw={700} size="lg" {...props}>
      Grupo Chegou
    </Text>
  );
}