from django.apps import AppConfig

class MetricasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'features.metricas'
    verbose_name = 'Métricas de Efetividade'